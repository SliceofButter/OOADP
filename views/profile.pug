extends layout

block content
  if user
    .container
      .row
        .col-md-4
          .card
            .card-header
              img(style='border-radius: 50%', width='100', height='100', src="/profilepic/" + pic)
              .details
                h5.name  #{user.username}
                  ul
                    li
        .col-md-8          
          .card
            .card-header
              | Bio
            .card-body #{bio}
            